# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# npm's lockfile. This project resolves with bun (bun.lock); an `npm install`
# run by a tool leaves one of these behind, and it must not be committed --
# two lockfiles disagreeing is how a build resolves versions nobody pinned.
package-lock.json
